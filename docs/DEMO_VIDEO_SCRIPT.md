# HazardLensAI — Demo Video Script (6:00)

**Format:** screen recording of the live app at http://localhost:3000 (record at 1440×900, browser zoom 100%).
**Accounts:** admin `admin@roadguardatlas.dev / Atlas@Admin2024` · citizen `citizen@roadguardatlas.dev / Atlas@User2024`.
**Pre-flight:** `bun run dev` (port 3000), seed fresh (`bun run scripts/seed.ts`), map tiles cached (visit `#/map` once before recording), close other tabs, hide bookmarks bar.
**Total runtime:** 6 minutes 00 seconds. Speak at a calm pace; every segment's narration is written to fit its slot.

| # | Time | Screen flow (URL / hashes & clicks) | Narration | Lower third |
|---|---|---|---|---|
| 1 | **00:00–00:20** Hook | `http://localhost:3000` landing — let the animated bbox scan play once | "In 2022, India recorded almost nineteen hundred deaths linked to potholes. Cities find out about these hazards from complaint calls — after the damage is done. What if every citizen's camera became part of the fix?" | **HazardLensAI** · Road Hazard Mapping using Vision, Geospatial Clustering & Maintenance Prioritization |
| 2 | **00:20–00:45** Solution in one breath | Scroll the landing page to the "how it works" strip; then click **Open the map** (`#/map`) | "HazardLensAI turns citizen photos into an auditable maintenance plan. Seven hazard classes detected by AI, verified by human moderators, grouped on a map, and scored zero to one hundred on a fully explainable priority scale." | Detect → Verify → Cluster → Prioritize → Repair |
| 3 | **00:45–01:30** The public map | On `#/map`: pan across Bengaluru; hover the **Silk Board** cluster marker (pulses); click it — drawer opens listing the pothole members; toggle **Heat** overlay off/on briefly; click one hazard to open its card | "This is the public map of Bengaluru. Pulsing markers are clusters — here at Silk Board Junction, three reports of the same pothole corridor were grouped automatically by the DBSCAN engine, sixty meters, three-point minimum. Click any marker for the evidence: photos, detection confidence, severity, and the live priority score." | Public map · 3 clusters · DBSCAN 60 m / 3 pts / 90 d |
| 4 | **01:30–02:30** Citizen reports a hazard | Open new tab → sign in as **citizen@roadguardatlas.dev / Atlas@User2024** (`#/signin`) → **Report a hazard** (`#/report`) → Step 1 upload `sample-data/images/pothole-1.jpg` → wait for AI preview (boxes + "pothole 9x%") → Step 2 keep the suggested class, set severity 4, drag pin onto Hosur Road, add note "Two-wheelers swerve into the fast lane here" → Step 3 submit → show confirmation with reference code | "Reporting takes three steps. Upload a photo — the AI preview draws boxes and suggests a class and severity. The citizen can correct the AI with one tap; those corrections become training data. Then place the pin, describe the hazard, and submit. This report just got its own reference code: RG-prefixed, quotable to the municipality." | 3-step wizard · AI preview · EXIF always stripped · GPS only with consent |
| 5 | **02:30–03:00** Privacy beat | Still in the report confirmation / dashboard: open **Profile → Privacy** and point at the location-consent toggle and notification settings | "Privacy is enforced in code, not policy alone: EXIF metadata is always stripped from stored media, GPS is read only with this consent toggle, and media lives behind unlisted links. Guests can even report without an account." | EXIF stripped · consent revocable · unlisted media ids |
| 6 | **03:00–04:15** Admin review & override | Sign out → sign in as **admin@roadguardatlas.dev / Atlas@Admin2024** → Admin (`#/admin`) → KPI tiles (28 actionable / 8 pending / 3 clusters) → open the newest pending report in **Review**: bbox evidence, detection panel, priority factors → **Edit** severity → click **Approve** with note "Verified against street imagery" → show toast + notification sent → open **Audit** tab, highlight the new `review.approve` row | "Now the moderator's side. Every pending report arrives with bounding-box evidence and a factor-by-factor priority explanation. The admin can correct any field before deciding, then approve, reject, flag — or merge duplicates by reference. Each decision notifies the reporter and lands in the audit log with actor, action and IP. Nothing consequential happens silently here." | Human-in-the-loop · merge by reference · audited overrides |
| 7 | **04:15–05:00** Priority engine & clusters | Admin → **Clusters** tab → click the Silk Board cluster → open **priority explanation** of the top member (66.6 · HIGH) — walk the five factors on screen → then **Work Orders** board: drag one card from `APPROVED` to `SCHEDULED`, add note "Storm-water division scheduled before next spell" | "Every hazard is scored zero to one hundred: severity weighted thirty-two percent, cluster density twenty-four, road criticality eighteen, recurrence fourteen, and unresolved age twelve. This highway pothole totals 66.6 — band HIGH. Field crews can override with a documented reason; the original math is never overwritten, just snapshotted. Approved work flows onto a kanban board from Reported to Resolved." | Priority = 0.32·sev + 0.24·den + 0.18·crit + 0.14·rec + 0.12·age |
| 8 | **05:00–05:35** Ops & observability | Admin → **Analytics** (by-class bar, severity-over-time, priority bands) → click **Export CSV** (download lands) → visit `http://localhost:3000/api/health` then `http://localhost:3000/api/metrics` (scroll the Prometheus text) | "For operators: analytics over classes, wards and priority bands, one-click exports to CSV and GeoJSON for GIS teams, a health endpoint, and Prometheus metrics out of the box. The full REST surface ships with an OpenAPI three-point-one spec and an embedded Swagger UI." | Analytics · CSV / GeoJSON / PDF · /health · /metrics · OpenAPI 3.1 |
| 9 | **05:35–06:00** Close + CTA | Landing page (`/`), hero shot; then a closing card with repo + docs links | "HazardLensAI — citizen cameras in, auditable repair plans out. The docs hub, model card, system card, API guide and full academic report are in the repository. Star the repo, try the demo, and bring it to your ward." | **github.com/&lt;org&gt;/roadguard-atlas** · docs/ · Try the demo · MIT-friendly research license |

**Production notes**
- Keep each click ≥ 1 s on screen before the next; zoom (140–160%) on: bbox preview (segment 4), priority factors (segment 7), audit row (segment 6).
- Add cursor-highlight (yellow circle) throughout; cut dead air between steps but keep narration continuous.
- Background music under −24 dB from 00:00–00:20 and 05:35–06:00 only; no music under narration in the middle.
- Captions: burn the lower thirds as specified; full subtitles recommended for accessibility.
- Fallback if live inference is slow during segment 4: pre-run the upload once before recording so the preview is cached; if the GLM engine stalls, the demo-engine fallback still draws boxes — do not stop the recording for it.
