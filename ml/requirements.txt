# RoadGuard Atlas ML pipeline — Python 3.10+ (tested on 3.12)
#
# CPU-only quickstart (recommended for laptops / CI):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
# GPU (CUDA 12.x): install torch/torchvision from the default index or
#   https://download.pytorch.org/whl/cu121 BEFORE this file, then
#   pip install -r requirements.txt  (torch pins are satisfied, wheels reused).
#
# NOTE on torch: requirements below intentionally do NOT pin torch to avoid
# pulling the ~2.5GB CUDA wheel onto CPU-only machines; the CPU index install
# above satisfies the requirement. If you install nothing first, pip resolves
# torch from PyPI (CUDA-bundled on Linux).

torch>=2.2
torchvision>=0.17
ultralytics>=8.2
opencv-python-headless>=4.8
mlflow>=2.12
onnx>=1.15
onnxruntime>=1.17
scikit-learn>=1.3
numpy>=1.24
pyyaml>=6.0
tqdm>=4.65
pillow>=10.0
python-dotenv>=1.0

# Inference service (ml/inference_service.py) + tests
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
pytest>=8.0
httpx>=0.27
